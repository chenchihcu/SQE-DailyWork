# Risk Ledger (SQE DailyWork)

| Scope | Risk | Guardrail | Next action (Owner=self) | Revalidation gate | Rollback | Status |
|:---|:---|:---|:---|:---|:---|:---|
| Data/Visit Records | 新增 `visit_product_sections` / `visit_defect_notes` 後，既有單產品訪廠可能顯示不一致 | 保留 `visits.product_id/product_name/...` snapshot；`create_schema()` backfill 舊訪廠為一筆產品區段；repository tests 覆蓋 legacy payload | 下次正式匯入/開啟既有資料庫時確認訪廠列表與 PDF | `scripts\verify.ps1` + legacy DB smoke open | 回退 repository/UI/PDF/docs 變更，保留 DB 備份 | Active |
| UI/Stats | 數據點密集時標籤產生重疊 (Label Overlap) | 1. 調小字級至 9pt<br>2. 保留 Tooltip 詳情作為冗餘顯示 | 若使用者反應遮擋嚴重，實作標籤碰撞檢測或僅顯示 Top 10 | 使用者驗證 (UAT) | 撤銷 `stats_view_widget.py` 中 `setPointLabelsVisible(True)` 之修改 | Active |
| UI/Stats | 渲染效能 (Rendering Performance) | QtCharts 原生優化 | 監控極大數據量（>50 點）時的流暢度 | 壓力測試 | 移除標籤顯示 | Low |
| UI/Stats/Period Filter | 篩選元件更換後，無法單月篩選 | 起訖年月為真實可見控制，允許相同月份；已移除以 dummy proxy 當功能證據的假設 | 維持同月、反向區間與原生 popup 回歸 | `test_date_range_and_export_warnings.py` + native popup belt | 回復 period-control helper | Mitigated |
| UI/Anomaly No | 手動編輯異常單號可能造成重複或格式錯亂 | Repository 單一 validator 強制 11 碼純數字、日期前綴、唯一性；UI 保留即時提示 | 收集使用者實務操作回饋 | `test_anomaly_no_insert_retry.py` + `test_anomaly_repository_invariants.py` | 回復 repository validator 與相關 UI wiring | Mitigated |
| Data/Backup And Verification | WAL raw copy 或測試直連正式庫會遺漏資料或造成正式資料變更 | SQLite online backup + read-only integrity/count parity；`SQE_DB_PATH` 單一解析；focused/full/native/baseline wrapper 強制 disposable DB，並在前後比對正式 DB 的 schema + 全表資料邏輯 SHA-256 | 每次相關 gate 必須顯示來源為 disposable，且正式 fingerprint 前後一致 | `test_database_backup.py` + `test_database_isolation.py` + `scripts\verify.ps1` + `scripts\verify_case_actions_phase1.ps1` | gate 立即 `not pass`；保留事件狀態快照後，以已驗證 pre-migration backup 回復 | Mitigated |
| Data/Legacy Migration | 逐筆錯誤後仍標記 migration 完成會永久漏資料 | 單一 transaction、錯誤 rollback、不寫 completion metadata、輸出 reconciliation | 只在拋棄式 legacy fixture 重跑 | `test_migration_atomicity.py` | 回復 migration phase；正式庫不得執行 | Mitigated |
| Data/Case Action Promotion | 新版本若在一般啟動時自動把正式 `anomaly_actions` / `corrective_actions` 升級，可能在未備份、程式仍開啟或版本不匹配時留下不可回復的半套資料 | 既有正式 DB 缺 `case_actions_v1` 時在 writable bootstrap 前以 read-only preflight fail closed；只有 fresh/disposable DB 可自動建立，且 repository 依 connection main path 阻擋正式 DB。正式 CLI 同時要求 promotion + apply-confirmation markers，再執行 verified backup、single transaction、idempotency、integrity/FK/lineage/count reconciliation 與 focused smoke | Phase 1 Promotion 已完成並由 forensic audit、focused/full/native gates 驗證；Phase 2 attachment Promotion 亦於 2026-08-26 verified（idempotent skip） | `tests/test_case_actions_phase1.py` + `tests/test_database_isolation.py` + `scripts/verify_case_actions_phase1.ps1` + `scripts/verify_attachments_phase2.ps1` + Promotion smoke | 還原完整 migration 前備份與前一可用應用版本；不做 reverse SQL | Mitigated / promotion verified |
| Data/Phase 1 Premature Promotion Incident | 2026-08-24 15:32 在尚未收到 `繼續` 前，正式 DB 出現只有明示 Promotion CLI 會建立的 `sqe_v2_backup_case_actions_v1_*` 與 canonical schema；原 verifier 沒有正式 DB 前後邏輯指紋，focused wrapper 也未固定 disposable DB | 唯讀 forensic audit 證明既有業務 rows 未變、僅 migration schema/lineage 與衍生 cache `updated_at`；先保存事件後完整快照，再以 pre-migration backup 回復，回復 state SHA-256 完全一致。新增 path-aware migration guard、雙 marker、read-only startup preflight、全 wrapper formal fingerprint invariant | Phase 1 focused/full/native/forensic gates 已從 verified disposable copies 完成；事件快照保留，後續正式 schema Promotion 仍採 separate approval | `scripts\audit_case_actions_phase1_databases.ps1` + `scripts\rollback_case_actions_phase1_incident.ps1` + focused/full gates | 事件後快照 `sqe_v2_incident_post_case_actions_v1_20260824_155557.db` 保留；若回復驗證失敗則自動放回該快照 | Mitigated / restored pre-promotion |
| Data/Legacy Action Snapshot | canonical 切換後仍有舊 API 寫入，會使舊／新 Action 表分叉；直接 DROP 又會破壞回滾能力 | 新程式只走 `_case_action_service`；migration-installed trigger 阻擋舊表 `INSERT/UPDATE`；舊表及 `legacy_source + legacy_id` lineage 保留，未獲授權不 DROP | 每階段執行 old-symbol call-site grep 與 mapping reconciliation；至少一個正式版本穩定後另案評估刪除 | focused service/migration tests + source grep + Full verify | 解除 guard 僅限完整回復舊版時，並搭配 migration 前備份 | Mitigated |
| Data/Phase 2 Attachments | SQLite attachment metadata 與 legacy physical files／`captions.json` 可能分裂；新 note/action link 若直接寫入正式 DB 會造成半套 schema | disposable/fresh schema contract、read-only preview、atomic apply helper、同案件 FK 驗證、physical-only legacy projection、filename containment guard；正式 DB 未完成 Promotion 前不自動加欄位 | 2026-08-26 正式 Promotion verified（`ready: true`、apply `skipped: true`）；backup `sqe_v2_backup_anomaly_attachments_v1_20260826_155811.db`；audit `scratch/phase2r-attachment-audit.json` | `tests/test_attachments_phase2.py` + `tests/test_anomaly_attachment_panel.py` + `scripts/verify_attachments_phase2.ps1` + workbench/storage/Markdown/PDF focused tests + Full verify | 完整回復附件 Promotion 前 backup 與前一版程式；不做 partial reverse SQL | Mitigated / Promotion verified |
| Traceability/Original Phase 2 Items 14–19 | 原始 Phase 2 `14–19` 清單未取得，無法建立可驗證的 item-level mapping；自行補造 mapping 會形成不可驗證的 acceptance claim | Design-derived mapping 固定於 `docs/exec-plans/completed/2026-08-26-phase2-items-14-19-mapping.md` 與 `scripts/audit_phase2r_attachments.py` `phase2_items_14_19`；不宣稱還原原始 48 項標題 | 2026-08-26 design-derived 六項對照完成；若日後取得權威原始清單，另開 delta audit | `docs/exec-plans/completed/2026-08-26-phase2-items-14-19-mapping.md` + `scripts/audit_phase2r_attachments.py` + Phase 2R focused gates | 保留現行 implementation；delta 僅在原始清單出現後進行 | Mitigated / design-derived mapping |
| Data/Phase 3 Hypothesis Schema | 多層 Hypothesis 若半套落地會與 1:1 `anomaly_root_causes` 分叉；note 與 hypothesis 混用會破壞 timeline/export 語意 | Phase 3b RFC 推薦獨立 `anomaly_hypotheses`；正式 DB fail closed 至 Promotion CLI；promotion 至 Root Cause 不得自動 `已驗證`；證據鏈 read model 單一 helper | 2026-08-26 正式 Promotion verified（`ready: true`、`applied: true`）；backup `sqe_v2_backup_anomaly_hypotheses_v1_20260826_214151.db`；audit `scratch/phase3-hypothesis-audit.json`；既有 37 筆異常未改寫 | `tests/test_hypothesis_phase3.py` + `scripts/verify_hypothesis_phase3.ps1` + `scratch/phase3-hypothesis-audit.json` | 完整回復 hypothesis Promotion 前 backup；不做 partial reverse SQL | Mitigated / Promotion verified |
| Traceability/Original Phase 3 Items 20–23 | 原始 Phase 3 `20–23` 清單未取得；自行補造 mapping 會形成不可驗證的 acceptance claim | Design-derived mapping 固定於 `docs/exec-plans/completed/2026-08-26-phase3-items-20-23-hypothesis-contract.md`；items 22–23 implementation complete | 2026-08-28 Phase 8 doc sync + SSOT fixes verified | Phase 3 + Phase 8 focused gates | 保留現行 implementation | Mitigated / design-derived mapping |
| SSOT/Phase 8 Pre-Release | Overview 缺 repeat、逾期雙口徑、區間匯出缺追溯欄、假設樹 reparent/audit、UI 動作閘控、主管檢視視覺缺口、`product_records` VIEW 未過濾 inactive | `get_anomaly_overview_card()` + `is_case_action_overdue` SSOT；contract tests；`manager-view` probe；`product_records` VIEW `WHERE is_active = 1` + Promotion CLI | 2026-08-28 Phase 8 remediation + formal VIEW Promotion verified | focused tests + native manager-view baselines + `test_product_records_view_write_path` + `scratch/product-records-view-audit-post.json` | 還原 Promotion 前 backup `sqe_v2_backup_product_records_view_is_active_v1_20260828_204019.db` | Mitigated / implementation verified |
| QA/Coverage Baseline | Phase 1–7 擴充後 line coverage 72.3% 低於舊 fail-under 79% 會誤殺 CI | 2026-08-28 依 `scratch/coverage-summary.json` 重標 `docs/release/coverage-baseline.json`（baseline 72.3%、fail-under 71.0%、core module mins 同步） | 2026-08-28 recalibration verified via `assert_coverage_baseline.py` | `verify.ps1 -Profile Coverage` | 回復舊 baseline 僅在刻意收緊 gate 時 | Mitigated / recalibrated |
| UI/Phase 4 Workbench Closure | Header 結案／重開若未寫 audit 會使 Timeline/History 與實際狀態分叉；結案附件若維持 legacy `AttachmentEditor` 會與 Phase 2 metadata 契約分裂 | Phase 4：`CASE_CLOSED`/`CASE_REOPENED` audit 與 close/reopen 同事務；`CloseAnomalyDialog` 改嵌 `EvidenceAttachmentPanel`；overview 品質結論只讀 `get_anomaly_overview_card()` | 2026-08-26 Phase 4 implementation verified（`verify_workbench_phase4.ps1` PASS） | `tests/test_workbench_phase4.py` + `scripts/verify_workbench_phase4.ps1` + native dialog-density baselines | 回復 Phase 4 前 UI/service 變更；無 DB rollback | Mitigated / implementation verified |
| Data/Phase 5 Repeat Issue | 即時全表掃描相似度會拖慢工作台；未 Promotion 的正式 DB 若自動建表會違反 Phase 1–3 gate 模式 | `anomaly_repeat_links` 索引表 + 供應商範圍 refresh；正式 DB fail-closed；Promotion 需雙 marker + backup | 2026-08-26 formal Promotion verified（22 links；backup `sqe_v2_backup_anomaly_repeat_links_v1_20260826_223506.db`） | `tests/test_repeat_issue_phase5.py` + `scripts/verify_workbench_phase5.ps1` | 還原 Promotion 前 backup | Mitigated / Promotion verified |
| UI/Phase 6 Manager View | 案件總覽若逐列呼叫 overview card 可能在大量 open 案件時拖慢頁面；作業清單僅涵蓋 supplier-event `case_actions` | Read model 無 schema；列表規模假設 manager-scale；NCR 線不併入 queue；雙擊路由工作台；`manager-view` native visual baseline | 2026-08-28 Phase 8：NCR boundary test + probe target + overdue SSOT 統一 | `tests/test_manager_view_phase6.py` + `qt_visual_regress.py --target manager-view` | 回復 Phase 6 UI/service 變更；無 DB rollback | Mitigated / implementation verified |
| Export/Phase 7 SSOT Parity | Excel/PDF/Markdown/PPTX 若各自重算逾期或品質欄會與工作台分叉；hypothesis PNG grab 失敗或區間報表大量嵌入會拖慢匯出或產生不完整報告 | 匯出層只讀 `get_anomaly_overview_card()`；hypothesis PNG 最多 12 張 + `export_include_charts` gate；失敗走「完成但有警告」；週報 overdue 用 overview SSOT | 2026-08-27 Phase 7 verified（`verify_exports_phase7.ps1` PASS；release 1.2.0 unsigned onedir + portable smoke） | `tests/test_exports_phase7.py` + `scripts/verify_exports_phase7.ps1` + Full verify | 回復 Phase 7 export/service 變更；無 schema rollback | Mitigated / implementation verified |
| Data/Product Ownership | 一筆正式異常的 supplier/product 歸屬無法從現有 master data 唯一判定 | 不自動修正；輸出 `Outputs/audit/*VERIFY.csv` 供人工分類 | 使用者確認歸屬後另開受控資料修正 | 正式庫唯讀 reconciliation query | 不適用；目前未寫入 | Active / VERIFY |
| Data/Phase0 Raw Hash | Phase 0 與結案的正式庫 raw hash 不同，但所有業務列一致；差異僅兩筆衍生月快取 `updated_at`，且結案值較早，現有證據無法唯一歸因 | 不自動還原、不寫正式庫；以 read-only row parity、integrity、當前 hash 穩定性保留證據 | 若需追查，先確認是否有外部程式同時開啟 DB，再以 Phase 0 快照做受控 cache reconciliation | Phase 0 snapshot 對正式庫逐表唯讀比較 | 未經使用者核准不還原 | Accepted residual / no live write |
| UI/Native Visual Gate | theme、probe target 或 baseline 漂移會讓單測／visual gate 假綠燈 | manifest-driven target mapping、缺 baseline 失敗、正式 theme 測試、Windows 100/125/150% belt | UI 變更後更新並人工檢視 baseline | `qt_visual_belt.py` + `qt_visual_regress.py` + harness mapping | 回復 UI helper／baseline 變更 | Mitigated |
| Performance/Startup Latency | 模組頂層靜態匯入 heavy 套件 (openpyxl/reportlab/matplotlib) 或重複查詢導致冷啟動延遲 | 1. 禁止頂層靜態匯入，改於業務函式內 lazy import<br>2. `export_helpers.py` 樣式快取<br>3. `LazyPageWidget` 延遲掛載<br>4. MainWindow 啟動去重 | 持續監控冷啟動秒數與匯入鏈 | 啟動性能回歸與單元測試 | 恢復靜態匯入並尋求其他非侵入性優化 | Mitigated |
| Export/PDF CJK Font | ReportLab 預設字型在中文 PDF 匯出時引發編碼錯誤或方框缺字 | 實作 `_register_cjk_fonts()` 自動掃描並註冊 Windows 微軟正黑體/細明體，樣式指定 CJK 字型 | 驗證各 Windows 環境與無字型環境之 fallback | `test_services_smoke.py` + PDF 匯出驗證 | 回退 PDF 匯出模組變更 | Mitigated |
| UI/Preferences Schema v5 | 偏好設定擴充至 27 欄位 (v5) 可能使舊版 v1~v4 JSON 解析失敗 | `AppearancePreferences.from_mapping` 提供嚴格型別校驗與記憶體向下相容 fallback，不污染/損壞儲存資料 | 覆蓋 v1~v5 mapping 轉換單元測試 | `test_appearance_preferences.py` | 回復偏好契約版本 | Mitigated |
| UI/NCR Embedding Smoke | `test_ncr_embedding_smoke` 假設 `content_scroll.widget() is fields_widget`；NCR create 已包 `NcrCreateFormContent` wrapper | 更新測試斷言為 scroll 子樹包含 `fields_widget`，勿在未確認 DOM 時假綠燈 Focused verify | 維持 wrapper DOM 與測試同步 | `test_ncr_embedding_smoke.py` + `scripts\verify.ps1 -Profile Focused` | 回復測試斷言或 NCR create DOM | Mitigated |
